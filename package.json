{
  "name": "learnstar-website",
  "version": "1.0.0",
  "description": "班宠乐园官网",
  "scripts": {
    "dev": "npx tailwindcss -i input.css -o dist/style.css --watch",
    "build": "npx tailwindcss -i input.css -o dist/style.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4"
  }
}
